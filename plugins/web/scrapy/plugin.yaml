name: web-scrapy
version: 1.0.0
description: "Scrapy web extraction fallback — no API key required. Used automatically when primary extract provider returns empty content. Install with `pip install scrapy`."
author: NousResearch
kind: backend
provides_web_providers:
  - scrapy