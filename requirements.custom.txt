# Pins match latest PyPI versions checked 22-07-2026 (venv, Python 3.14).
# playwright version MUST match the Docker base image tag in Dockerfile.api / Dockerfile.scanner.

# Browser automation
playwright==1.61.0

# Firecrawl (client code written against 4.x API)
firecrawl-py==4.32.1

# Browser-use (AI browser control — ships its own LLM clients, no langchain needed)
browser-use==0.13.6

# FastAPI + DB
fastapi==0.139.2
uvicorn==0.51.0
asyncpg==0.31.0
psycopg2-binary==2.9.12
sqlalchemy==2.0.51
python-dotenv==1.2.2

# HTML parsing (connectors use bs4)
beautifulsoup4==4.15.0

# PDF parsing (processor/pdf_parser.py uses pdfplumber)
pdfplumber==0.11.10
pymupdf==1.28.0

# HTTP
aiohttp==3.13.4  # browser-use 0.13.6 hard-pins this exact version
httpx==0.28.1

# Scheduling
apscheduler==3.11.3

# Data
pydantic==2.12.5  # browser-use 0.13.6 hard-pins this exact version
python-dateutil==2.9.0.post0  # portals render deadlines in a dozen formats

# Hermes inference router (agent.auxiliary_client.call_llm) needs PyYAML to read
# config.yaml; openai/anthropic SDKs already ship in the base image.
pyyaml==6.0.2
