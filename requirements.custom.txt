# Pins match versions tested locally on 16-07-2026 (venv, Python 3.14).
# playwright version MUST match the Docker base image tag in Dockerfile.api / Dockerfile.scanner.

# Browser automation
playwright==1.61.0

# Firecrawl (client code written against 4.x API)
firecrawl-py==4.32.0

# Apify (client code written against 3.x Run objects)
apify-client==3.0.6

# Browser-use (AI browser control — ships its own LLM clients, no langchain needed)
browser-use==0.13.4

# FastAPI + DB
fastapi==0.111.0
uvicorn==0.30.0
asyncpg==0.29.0
psycopg2-binary==2.9.9
sqlalchemy==2.0.30
python-dotenv==1.0.1

# HTML parsing (connectors use bs4)
beautifulsoup4==4.12.3

# PDF parsing (processor/pdf_parser.py uses pdfplumber)
pdfplumber==0.11.0
pymupdf==1.24.5

# HTTP
aiohttp==3.9.5
httpx==0.27.0

# Scheduling
apscheduler==3.10.4

# Data
pydantic==2.7.0
